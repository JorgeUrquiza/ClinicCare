import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    listContainer: {
        justifyContent: 'center',
        width: 350,
        marginTop: 15,
        marginBottom: 110,
        borderColor: 'white',
      },
})